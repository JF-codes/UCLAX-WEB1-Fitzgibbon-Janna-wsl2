import styled from "styled-components";

const Detail = ({ detail }) => {
    return <DetailStyled className="Detail">{detail}</DetailStyled>;
};

export default Detail;

const DetailStyled = styled.li``;
